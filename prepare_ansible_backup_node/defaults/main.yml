---
# defaults file for prepare_ansible_backup_node

platforms:
  - mikrotik
  - cisco
  - palo_alto
current_date: "{{ lookup('pipe','date +%Y-%m-%d') }}"
yesterday_date: "{{ lookup('pipe','date +%Y-%m-%d --date=yesterday' ) }}"
